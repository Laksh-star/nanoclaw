# BizClaw Environment Configuration
# Copy this file to .env and fill in your values
# cp .env.example .env

# ─── Instance ────────────────────────────────────────────────────────────────

# Name for this deployment — used in service name, logs, and container image.
# Use lowercase letters, numbers, hyphens only. No spaces.
# Example: acme-corp, client-ngmf, my-business
INSTANCE_NAME=bizclaw

# Assistant name — what users call the bot in chat (default: Andy)
# Example: @Andy, @Biz, @Rex
ASSISTANT_NAME=Andy

# ─── Claude / Anthropic ──────────────────────────────────────────────────────

# Get from: claude setup-token (for Claude Pro/Max subscribers)
CLAUDE_CODE_OAUTH_TOKEN=

# OR use an Anthropic API key instead of the OAuth token
# ANTHROPIC_API_KEY=

# ─── Multi-Model AI (Optional) ───────────────────────────────────────────────

# OpenRouter API key — enables non-Claude models (Kimi, Gemini, etc.)
# Get from: https://openrouter.ai/keys
OPENROUTER_API_KEY=

# Default model used when call_model is invoked without specifying a model
# Good options: moonshotai/kimi-k2.5, google/gemini-2.0-flash-001
OPENROUTER_DEFAULT_MODEL=moonshotai/kimi-k2.5

# ─── Web Search (Optional) ───────────────────────────────────────────────────

# Tavily API key — structured web search with source citations
# Get from: https://tavily.com
TAVILY_API_KEY=

# ─── Channels ────────────────────────────────────────────────────────────────

# Telegram bot token — required if using Telegram channel
# Get from: @BotFather on Telegram
TELEGRAM_BOT_TOKEN=

# Set to true to use Telegram only (no WhatsApp)
# TELEGRAM_ONLY=false

# Set to true if the assistant has its own dedicated phone number
# (vs using your personal number via self-chat)
# ASSISTANT_HAS_OWN_NUMBER=false

# ─── Voice Transcription (Optional) ─────────────────────────────────────────

# OpenAI API key — used for Whisper voice transcription
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# ─── Advanced (Optional) ─────────────────────────────────────────────────────

# System timezone for scheduled tasks (cron expressions)
# Default: system timezone. Example: Asia/Kolkata, America/New_York, UTC
# TZ=Asia/Kolkata

# Max containers running simultaneously (default: 5)
# MAX_CONCURRENT_CONTAINERS=5

# Container idle timeout in ms — how long to keep a container alive after last response
# Default: 1800000 (30 minutes)
# IDLE_TIMEOUT=1800000

# Log level: trace, debug, info, warn, error (default: info)
# LOG_LEVEL=info
